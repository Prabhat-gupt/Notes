import { Router } from 'express';
import { Config, Package } from '@verdaccio/types';
import { IAuth, IStorageHandler } from '../../../types';
export declare type PackcageExt = Package & {
    author: any;
    dist?: {
        tarball: string;
    };
};
declare function addPackageWebApi(storage: IStorageHandler, auth: IAuth, config: Config): Router;
export default addPackageWebApi;
