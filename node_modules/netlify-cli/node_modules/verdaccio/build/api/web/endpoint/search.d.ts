import { Router } from 'express';
import { IAuth, IStorageHandler } from '../../../types';
declare function addSearchWebApi(storage: IStorageHandler, auth: IAuth): Router;
export default addSearchWebApi;
