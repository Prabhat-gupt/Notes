import { Router } from 'express';
import { Config } from '@verdaccio/types';
import { IAuth } from '../../../types';
declare function addUserAuthApi(auth: IAuth, config: Config): Router;
export default addUserAuthApi;
