import { Router } from 'express';
import { IAuth } from '../../../../types';
export interface Profile {
    tfa: boolean;
    name: string;
    email: string;
    email_verified: boolean;
    created: string;
    updated: string;
    cidr_whitelist: string[] | null;
    fullname: string;
}
export default function (auth: IAuth, config: any): Router;
