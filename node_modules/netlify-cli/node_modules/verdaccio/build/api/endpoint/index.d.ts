import { Config } from '@verdaccio/types';
import { IAuth, IStorageHandler } from '../../types';
export default function (config: Config, auth: IAuth, storage: IStorageHandler): import("express-serve-static-core").Router;
