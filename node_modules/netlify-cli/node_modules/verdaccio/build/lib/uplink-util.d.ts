import { Config, Versions } from '@verdaccio/types';
import { IProxy, ProxyList } from '../types';
/**
 * Set up the Up Storage for each link.
 */
export declare function setupUpLinks(config: Config): ProxyList;
export declare function updateVersionsHiddenUpLink(versions: Versions, upLink: IProxy): void;
